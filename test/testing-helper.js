/** @module Testing Helper. Used to run common test initialization before testing begins */
const helper = require('@major-mann/base-lib').testingHelper;
// This initializes chai middleware and adds chai and expect to the global object
helper();
